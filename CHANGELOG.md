# Changelog

## 1.7.1-rc1 — 2026-09-07

- UV面コーナーの独立トポロジー診断を追加。UVの点接触・不正共有・3面以上のエッジ共有を検出して既存の修復へ渡す。
- カットするエッジが空でもUV割り当ての再構築を試行。
- 修復後のUnfold3D実行を任意指定できるチェックボックスと `repair_and_unfold_selected()` を追加。
- 修復中は全UVセットの既存コーナー座標を検証。展開後は形状・UVトポロジー・他UVセット座標を検証し、失敗時は同一Undo単位のロールバックを要求。
- 三角化・面削除・頂点移動は追加していない。非多様体形状の分離は既存の明示設定に従う。
- 平面投影→修復→展開の順序を案内。修復後に再投影しても常に再発しない、という案内を削除。
- Python回帰テスト66件PASS。今回のMaya実機確認は未実施。詳細はUV_FIX_1.7.1_JA.md。

## 1.7.0-rc1 — 2026-09-07

- 選択メッシュ向けに「UV展開エラーを修復（三角化なし）」を追加。事前の13項目検査は不要。
- 非多様体の形状接続を再検出しながら分離。UVの不正な共有はカットまたは既存座標を保持して割り当てを再作成。未割当面だけ平面投影を作成可能。
- 面数、各面の頂点数・並び・位置の一致と、MayaのUnfold事前検証を確認。失敗時は全体を巻き戻し、成功時もUndo 1回で復元。
- 一括修復の「三角化を許可する」を初期オフに変更。
- 指定のwig OBJで四角形24,027面を保持したまま非多様体を解消し、平面投影後のUnfold成功を確認。

## 1.6.0-rc1 — 2026-09-07

- 「すべての問題を修復…」ボタンと全結果の操作一覧を追加。検索フィルターや行選択に依存せず全検査対象を処理。
- 各編集前の再検出により、変更後のコンポーネントIDを使用。重複面は1枚を残して整理。
- 穴埋め・法線修正・面削除の設定を追加。全面削除を避け、解消できない問題を結果に残す。
- 一括処理全体を1つのUndoにまとめ、途中失敗時の全体巻き戻しと終了後の再検査を実装。

## 1.5.0-rc1 — 2026-09-06

- 問題選択時にMayaの選択モード・ハイライト・マスクを同期。複数種類のコンポーネントはF7相当の複合選択を使用。
- 表示中のビューポートを解決し、フォーカスを即座に実行。
- 個別修復画面を追加。コンポーネントの選択、適用範囲の確認、三角形化・マージ・縮約・法線修正・穴埋め・面削除に対応。
- 全面削除時は対象メッシュ形状を明示的に削除。Undo・失敗時の巻き戻し、古いプレビュー・共有形状の誤編集防止を追加。
- 自動確認オフでもツールからの編集後は1回再検査し、設定を維持。変化がない場合も古い表示の待機状態を解除。

## 1.4.0-rc1 — 2026-09-06

- フェイス選択・選択モード／マスク変更時にVP2描画を明示更新。選択コンポーネントと併存する深度優先度へ変更。
- 「修正を自動確認」を追加。編集停止700ms後に変更メッシュだけを再検査し、解消箇所を一覧・描画・ホバーから除外。
- 検査時の設定を保持し、Undo／Redo・リネーム・削除後の復元に追従。全問題の解消後も対象を監視。
- 解析中の追加編集による古い結果を破棄。失敗・未完了を解消済みとして扱わない。
- ウィンドウ終了・再読み込み・シーン変更時のタイマー／コールバック／ワーカー停止を追加。

## 1.3.0-rc1 — 2026-09-06

- ビューポートの「マウスで詳細」を追加。重要度・問題名・コンポーネント番号・説明をカードに表示。
- 手前のポリゴン面へのレイ判定とフルDAGパスで照合。未検査メッシュによる遮蔽、裏面、同名メッシュ、インスタンス、同一深度の曖昧な重なりを考慮。
- 結果フィルターと描画上限に合わせてホバー索引を更新。同じ面の別検査項目へ切り替えた場合も説明を更新。
- 操作中は非表示、閉じると監視停止。ホットリロード・Maya終了時にタイマーと終了コールバックを破棄。

## 1.2.0-rc1 — 2026-09-06

- 3Dベクトル演算とAABB判定のPython反復コストを削減。同じ中規模テストで解析時間を約43%短縮。
- 通常表示では裏向きの面・エッジ・頂点を隠し、深度判定で手前の形状による遮蔽を反映。
- 「隠れた箇所も表示」を初期オフに変更。旧版のオン設定も初回はオフへ移行。
- 面の中心マーカーを縮退面に限定し、線幅・点サイズ・塗り濃度を抑制。
- ID文字にもポリゴンメッシュによる遮蔽判定を追加。
- 表示設定の切り替えで描画ノードとスナップショットを再利用。カメラ変更時も頂点配列を維持。
- ホットリロード時に描画プラグインも解除し、次の表示で修正版をロード。

## 1.1.1-rc1 — 2026-09-06

- 選択・選択解除・コンポーネントモード切り替えで問題箇所の表示が消える不具合を修正。
- `timeChanged`通知は実際の時刻を比較し、同じフレームでの通知では表示を維持。
- メッシュのdirty通知はidleまでまとめ、評価後のスナップショットに実際の変化がある場合のみ無効化。
- 頂点・変換・履歴・フレームの実変更時の無効化は維持。
- `reload_ui()`でビューポートのセッションとコールバック実装も更新。
- Maya 2026・2027のオーバーレイ回帰テスト各17件、Qtのホットリロード、通常起動Maya 2026での6種類の選択操作を確認。

## 1.1.0-rc1 — 2026-09-06

- UI全体の100〜200%拡大と倍率保存、画面を超える場合のスクロールを追加。
- Viewport 2.0に問題の面・エッジ・頂点を重要度別カラーで描画。
- 全結果／選択行のみの表示、透過表示、ID表示、表示解除を追加。
- 元の形状・マテリアル・選択を維持し、描画ノードをシーン保存から除外。
- 形状・変換・フレーム変更時に表示を無効化し、古い検査位置の残存を防止。
- 起動中のMayaに更新を反映する`reload_ui()`を追加。

## 1.0.0-rc1 — 2026-09-06

- 13項目のメッシュ検査、距離・面積・細長さのしきい値を実装。
- 日本語Qt UI、プリセット、検索・重要度フィルター、ネイティブ選択を実装。
- Maya Qt6/PySide6とQt5/PySide2のインポート切り替え。
- AABB木による空間検索、候補比較上限、キャンセルと実行状態を実装。
- Mayaの穴付き面、選択階層とインスタンスパスを考慮。
- スナップショットの検証により編集後の古い結果の選択を防止。
- UTF-8のJSON / CSV / HTMLレポート、サンプルシーン、テストを同梱。
