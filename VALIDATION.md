# 1.7.1-rc1 今回の検証状況

- 環境: Linux / Python 3.13。Maya・mayapy・PySideは利用不可。
- `python3 -m unittest discover -s tests -p 'test_*.py' -v`: 66件PASS（追加25件、既存41件）。
- 追加分は独立UV接続診断15件、Mayaコマンドをモック化した制御フロー10件。モックの成功は実機の修復成功・Undo復元を証明しません。
- 全Pythonファイルの構文コンパイルを実施。
- `tests/integration_uv.py` に今回の実機向け回帰テストを追加しましたが、未実行です。
- UI表示・Unfold3D・複雑な履歴・複数UVセット・スキン等は実機未確認。
- 添付はツール本体のみで、問題の発生したメッシュデータは含まれていないため、症状の解消は未確認です。
- 以下は受領ZIPに含まれていた旧版の検証記録です。この環境で再実行した結果ではありません。

---

# 検証記録

対象: Mesh Sentinel 1.7.0-rc1 / 2026-09-07 / Windows。

## 1.7.0のUV修復 — 2026-09-07

- Maya 2026・2027 mayapyで `tests/integration_uv.py` 各10件PASS。四角形・面の頂点位置保持、ネイティブUnfold実行、UVだけの不正共有、既存UV座標保持、別UVセット保持、部分未割当UV、Undo、古い診断拒否、処理失敗時の巻き戻し、未検査状態からのQt修復操作を確認。
- 一括修復回帰は2026・2027とも各7件PASS。三角化は明示オン時だけ実行するようテストを更新。
- 指定OBJは元データに非多様体頂点813・エッジ155を含み、UV割り当ては0。29,360頂点／26,345面（四角形24,027、既存三角形2,318）。接続分離9回で32,277頂点となり、面数・面ごとの頂点数・巻き順・位置は完全一致。三角形の追加なし、面削除なし。
- 修復済みOBJ・Maya ASCIIを別ファイルに保存。修復後の再平面投影Z→ `u3dTopoValid` PASS→ `u3dUnfold` 完了（約31秒）。元OBJのSHA-256不変。詳細は別納品 `wig_uv_fixed/validation.json`。
- 通常起動Maya 2026で実モデルを使い、ボタン→UV修復ダイアログ→修復→Undoによる元の形状・UV復元を検証。納品OBJを読み込み直し、平面投影→Unfoldも検証。
- この検証は未変形の静的ポリゴンを対象とする。スキン、ブレンドシェイプ等の全履歴の保持や最終テクスチャ配置は未検証。形状側の分離は頂点接続と境界を変更するため、後でマージすると再発し得る。

参照: Autodesk [Unfold用UVの準備](https://help.autodesk.com/cloudhelp/2025/ENU/Maya-Modeling/files/GUID-3FD8D325-22F1-44CC-9693-F45E5CE2AD4D.htm)、[polySplitVertex](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/polySplitVertex.html)、[polyMapCut](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/polyMapCut.html)。頂点分離は新たな非多様体を作る場合があるため毎回再検出し、上限で解消しなければ巻き戻す。

## 1.6.0の一括修復 — 2026-09-07

- Maya 2026・2027 mayapyで一括修復テスト各7件PASS。複数メッシュをUndo 1回で復元、古い後続ターゲットがある場合の変更前停止、2番目のメッシュの処理失敗による全体巻き戻し、重複面を1枚残す整理、削除の初期オフ・全面削除の回避、穴埋め後の新しい面IDでの三角形化を確認。
- Qtで検索フィルターに隠れている結果も対象に含めること、自動確認オフでも一括修復後に1回再検査し設定を保持することを確認。
- 通常起動Maya 2026でも新しいボタン→確認ダイアログ→2メッシュの修復→検出の解除→Undo 1回で両メッシュと検出結果の復元までPASS。Maya終了コード0。
- 既存の個別修復統合は14件PASS・GUI専用2件をスキップ。プレビュー生成では検査・選択・出力・キャンセルを再確認。メインUIと一括修復画面の実Qt画像を確認。
- 一括修復は有限の処理順で実行し、必ず全問題をゼロにできる保証はない。未解決・意図の判断が必要な問題は再検査後に残す。適用中はUndoに別操作が混ざらないようイベントループを再入せず、巨大メッシュでは操作待ちが発生する。

## 1.5.0の選択・個別修復

- 通常起動Maya 2026で修復統合16件PASS。単独／複合コンポーネント選択、三角形化、選択面削除、最終面削除、頂点から接続面への範囲展開、境界ループ全体の穴埋め、法線反転・カスタム法線解除、重複相手のマージ、ゼロ長エッジ縮約、古いプレビュー拒否、共有インスタンス拒否、Undo無効時の拒否、部分失敗の巻き戻しを確認。
- 通常MayaのUIで「問題箇所を選択」のモード・マスク・面IDを検証。「選択してフォーカス」で実際に透視カメラが移動することを検証。修復画面から三角形化し、再検査後に問題が消え、Undo 1回で元の五角形と問題表示が戻ることを確認。
- Maya 2027 mayapyの修復統合は14件PASS、GUI専用2件は明示スキップ。Maya 2026・2027のQt修復テストは、範囲プレビュー・未編集メッシュ維持・Undo／Redo・全フェイス削除・自動確認オフ時の1回更新・古いダイアログ拒否までPASS。
- 既存のMaya統合18件、Qt検査・エクスポート・選択・ホットリロード、自動確認の回帰テストもPASS。
- 選択マスクはオブジェクトモードのままAPI選択リストを設定する実装から、モード／ハイライト／マスクを設定して選択する実装へ変更。複合選択はMaya標準 `SelectMultiComponentMask` の `meshComponents` モードを利用。Mayaの選択リストでは同じDAG上の複数種類が1エントリーに集約されるため、リスト長ではなく実際の選択IDで検証。
- 最後の1面への面削除が無変更となるケースを確認し、全フェイスがプレビュー対象ならメッシュ形状を削除するよう修正。通常の部分削除は面のみを削除。
- 通常Mayaで一度Undo復元の検証が失敗したため、Undoの最上位コマンドも記録して再実行。最上位が修復チャンクであることと、1回での形状・検出結果の復元を確認。

範囲: Windows、Maya 2026の通常GUI、Maya 2026・2027 mayapy。実シーンのスキン・ブレンドシェイプ・多数のリファレンスを含む制作リグ、全種類のカスタム属性・材質・UVの保持は未検証。自己交差やTジャンクション等の自動リトポロジーは実装していない。

API参照: [選択モード／マスク](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/selectType.html)、[カメラの選択範囲へのフィット](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/viewFit.html)、[境界の穴埋め](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/polyCloseBorder.html)、[法線操作](https://help.autodesk.com/cloudhelp/2026/ENU/Maya-Tech-Docs/CommandsPython/polyNormal.html)。

## 1.4.0の選択描画・自動確認

- Maya 2026・2027で `tests/integration_live.py` PASS。フェイス選択、2メッシュの片方だけ修正、未編集結果の同一性、全解消、Undo／Redo、リネーム、削除・復元後の編集、解析中の追加編集による古い結果破棄、読み取り失敗、オン／オフ、終了処理を確認。
- 通常起動Maya 2026 / VP2のカラーバッファで、オブジェクト選択・F11後・全フェイス選択後のオーバーレイ表示を確認。各状態で表示なしとの比較は241,509／241,783／241,591サンプル画素に差分。
- 描画優先度変更後の遮蔽テストPASS。表174,251画素の差分、手前の立方体の中央領域では0、裏側では0。透過オン時の中央領域2,500／裏側279,841画素の差分。
- Qtの検査・出力・選択・設定・再読み込みテスト、ビューポート統合20件PASS（Maya 2026）。
- 通常起動Maya 2026でもUIから検査→F11→片方の修正→全解消→Undoを実行。問題件数2→2→1→0→1とオーバーレイの維持／解除／復元を確認し、終了コード0。ホバー統合15件も再確認してPASS。
- 旧描画でも単純な平面シーンでは消失を再現できなかったため、選択イベントの再描画と深度優先度を補強して検証。ユーザーの元シーン固有の条件、高DPI、Smooth Mesh Previewとの一致は未検証。
- Maya 2027の制限付き実行環境ではTemporaryDirectoryの削除時に権限エラー。テスト引数で作業用の通常ディレクトリを指定して再実行し、2026・2027とも終了コード0を確認。

## 1.3.0のホバー検証

- Maya 2026・2027でホバー統合テスト各15件PASS。正面、背面、背景、未検査の遮蔽物、同じメッシュ内の奥の面、同一深度で重なる別メッシュ、同名ノード、インスタンス、頂点・エッジのピクセル距離、非表示・フィルター・表示上限、選択維持、索引の破棄、同一面でのルール切り替えを確認。
- ビューポート統合20件、Qtの検査・出力・選択・設定・再読み込み・閉じる／再表示もMaya 2026でPASS。
- 通常起動Maya 2026 / VP2 / RTX 4080で実際のカーソル位置から説明カードを表示。N-gon・凹N-gonの説明が同じ面に対して表示され、フルパスと面IDが一致することを確認。
- 説明表示中も選択が変わらず、手前に未検査メッシュを追加するとカードが隠れることを確認。
- 検証途中のグローバルQtイベントフィルター使用版では、終了時にShiboken内でアクセス違反が発生。イベントフィルターを廃止し、タイマー監視と明示的な終了処理へ変更。修正後は同じ実ホバー検証がPASSし、Mayaプロセス終了コード0を確認。ユーザーの既存シーンは検証に使用していない。
- `preview_hover.png`は実ホバー中のカードをキャプチャしたもの。Qt本体のプレビュー・サンプルレポートも更新。

検証範囲: 実画面は通常の透視投影・100%ディスプレイ倍率で確認。隔離・オブジェクトフィルターのDAG照合はAPIリストを使った統合テスト。高DPI・複数画面・隔離モードの実画面操作、NURBS・GPUキャッシュ・透明材質・Smooth Mesh Previewとの描画一致は未検証。完全縮退などレイがポリゴン面に当たらない場合は誤表示を避けて説明を出さない。

API参照: [M3dViewの座標変換とビューフィルター](https://help.autodesk.com/cloudhelp/2026/ENU/MAYA-API-REF/py_ref/class_open_maya_u_i_1_1_m3d_view.html)。

## 1.2.0の高速化・可視性検証

- 幾何・レポート36件と、裏面判定の5件、計41件PASS。正面／背面、平行投影、縮退面、孤立頂点、表裏に接続した頂点を検証。
- Maya 2026・2027でビューポート統合テスト各20件PASS。選択時の表示維持、実編集時の解除に加え、表示設定変更でのノード・スナップショット・MPointArray再利用を確認。
- Maya 2026のホスト統合18件、Qtの設定移行・検査・出力・選択・再読み込みもPASS。更新後のQt実装をoffscreenで再描画し、結果画面を目視確認。
- 同じ5,041頂点／4,900四角形／9,800内部三角形の13項目解析: 変更前3.799秒→変更後2.170秒（約43%短縮）。両方とも完了し、外周280エッジのみ検出。単一実行環境での参考計測。Mayaのスナップショット取得時間と描画は含まない。
- **通常起動Maya 2026 / VP2 / RTX 4080でカラーバッファを比較**。検査用平面の前に立方体を置き、表からの表示、遮蔽、カメラを裏へ移動した際の自動更新、透過切り替えを確認。
- 通常モードの背面では、表示なし画像との有意な画素差が0。中央50×50pxの遮蔽領域も差が0。透過モードでは同領域2,500pxに差が発生し、切り替えが反映されることを確認。
- ID文字の有無の両方で実GPU確認。Mayaの文字描画は深度判定を無視するため、通常モードでは最大60個の文字アンカーに対しネイティブの交差アクセラレータで遮蔽を判定。別メッシュの移動・非表示への追従も統合テストで確認。
- `preview_visibility_front.png` / `preview_visibility_back.png`は通常モードの実描画。テスト用プロセスのみ使用し、元から開いているユーザーのシーンには接続していない。

可視性の範囲: 通常の不透明ポリゴンメッシュを対象に確認。裏面判定は面の面積加重幾何法線を使うため、極端な非平面ポリゴンでは部分的な表裏を分割しない。ID文字の遮蔽はポリゴン面を不透明として扱い、NURBS・GPUキャッシュ・プラグイン独自形状やパネル固有の隔離表示との一致は未検証。IDオフ（初期値）では文字用レイ判定は行わない。

API参照: [MUIDrawManagerのインデックス描画](https://help.autodesk.com/cloudhelp/2026/ENU/MAYA-API-REF/py_ref/class_open_maya_render_1_1_m_u_i_draw_manager.html)、[MFnMeshの交差アクセラレータ](https://help.autodesk.com/cloudhelp/2026/ENU/MAYA-API-REF/py_ref/class_open_maya_1_1_m_fn_mesh.html)。

## 1.1.1の修正検証

- Maya 2026・2027のオーバーレイ統合テストを各17件実施しPASS。
- 選択、選択解除、コンポーネントモード、表示設定のみの変更、同一フレーム通知で表示を維持。
- 頂点移動、変換、入力履歴編集、実フレーム変更時には表示を無効化。
- 連続dirty通知を1回のスナップショット確認に集約し、旧セッションの遅延処理が新しい表示を消さないことを確認。
- Maya standaloneにはGUIのidleループがないため、統合テストでは遅延処理をキューに保留してDG操作後に実行。
- **通常起動のMaya 2026でも実際のidle処理を確認**。オブジェクト選択、選択解除、頂点選択、コンポーネントモード、結果からの選択、オブジェクトモードの6操作すべてで表示を維持。修正途中の版で同一フレームの`timeChanged`による解除を再現し、修正後は6操作すべてPASS。
- Maya 2026付属Qtの操作フローもPASS。ホットリロードで旧セッションとコールバックを破棄し、修正済みセッションへ切り替わることを確認。
- ここでの選択操作はMayaコマンドを通常のGUIプロセスで実行して検証。マウスクリック自体の入力テストではありません。

## 1.1.0の追加検証

- Maya 2026・2027: オーバーレイの統合テストをそれぞれ9件実施しPASS。
- 元の形状・選択の維持、表示解除、一時ノードの保存除外、編集後の無効化、シーン切り替え時の後始末、古い結果の拒否を確認。
- 描画マネージャーへの面・線・点・透過描画・ID文字の呼び出しと、表示上限・重要度の重複整理を確認。
- Maya 2026付属Qt: 100→200→125%変更時のフォントサイズ、スクロール、設定保存を確認。小数倍率でQtの整数px制約を考慮して丸めています。
- Qtからの自動ビューポート表示、選択行への絞り込み、表示解除を確認。
- 開いているウィンドウを安全に作り直すホットリロードもQtテストで確認。
- **通常起動のMaya 2026 / Viewport 2.0 / NVIDIA RTX 4080**でも別の検証プロセスを起動し、サンプル38箇所の表示前後のカラーバッファを取得して目視確認。`preview_viewport.png`は実ビューポートのキャプチャです。
- 元から開いているユーザーのMayaシーンには接続せず、検証プロセスは終了しています。

## 実施済み

| 検証 | 環境 | 結果 |
|---|---|---|
| 幾何・状態管理・レポート | Maya 2026付属Python 3.11.9 | 36テスト PASS |
| Maya Python API統合 | Maya 2026 / mayapy | 18テスト PASS |
| Maya Python API統合 | Maya 2027 / mayapy | 18テスト PASS |
| Qt UI操作フロー | Maya 2026付属PySide6 / Qt 6.5.3、offscreen | 起動・検査・選択・検索・重要度・出力・中止 PASS |
| UI画像 | 同上、Windows既存フォントを明示ロード | 初期画面・結果・設定画面を目視確認 |
| 中規模メッシュ | 5,041頂点 / 4,900四角形 / 9,800内部三角形 | 13項目完了、想定した外周280エッジのみ検出、解析約3.97秒 |

計測値はこの実行環境での1回の参考値です。Mayaスナップショット取得時間を含みません。全形状・全ハードウェアで同じ性能を保証する数値ではありません。

## 回帰テストの内容

- 正常な立方体・球・隣接三角形に自己交差の誤検出がないこと。
- 13項目すべての陽性ケース。
- 共有頂点があっても交差がその頂点の外に延びれば検出すること。
- 共面重複、共有エッジの正常接触と折り重なりの区別。
- 重複頂点の近傍セル、距離閾値内外、逆巻き重複面。
- 内部の別シェル・単独面、境界上の面、容器を横切る面、開いた容器。
- 内向き閉殻、巻き順不整合、カスタム法線、遠方座標の符号付き体積。
- Mayaの穴付きポリゴンで外周・穴周囲すべての境界エッジを検出。
- 選択階層、コンポーネント選択、インスタンスのDAGパス、中間シェイプ除外。
- コンポーネントIDがMayaのネイティブ選択と一致。
- 頂点・ワールド変換・リネーム後の古い結果による選択を拒否。
- 表示単位を変更してもAPI座標のcmが変わらないこと。
- 検査によりシーンの形状・dirtyフラグを変更しないこと。
- 比較上限・中止を「検出なし」と扱わないこと。
- JSON読込、HTMLエスケープ、CSV出力と検査状態保存。
- フォーカス時にモデルパネルからカメラを解決する呼び出しを検証。

## 検証の限界

通常起動したMayaウィンドウ内でのドラッグ＆ドロップ、シェルフ登録後のMaya再起動、実ビューポートのフォーカス移動は、この記録の自動検証には含みません。フォーカスのカメラ解決は呼び出しのモック検証です。Qt画面は実装したウィジェットをoffscreen描画したもので、デザインモック画像ではありません。

Maya 2027の実GPU描画、異なるGPU/APIの描画差、他社の描画プラグインとの併用は未検証です。描画ノードはローカル用途のMTypeId `0x0007F71A`を使用します。スタジオ導入・広範な配布では既存プラグインとのID衝突がないことも確認してください。

Maya 2022〜2025、PySide2、macOS、Linux、数百万ポリゴン、複雑な参照・変形履歴、極端な浮動小数点条件の網羅的受け入れは未実施です。販売対象の対応表はこの検証範囲を超えて設定しないでください。

実行時にAutodesk CERログの書き込み先に関する診断が標準出力に現れました。上記テストはそれぞれ正常終了し、Maya APIとQtの結果を直接確認しています。

## 再実行

MeshSentinelフォルダー内で、専用のプロセスから実行します。

```text
python -m unittest discover -s tests -v
mayapy tests/integration_maya.py
mayapy tests/integration_ui.py
mayapy tests/integration_viewport.py
mayapy tests/integration_hover.py
```

UIテストはPySide6環境用です。`QApplication`をMaya standaloneの初期化より先に作成します。通常のMaya GUI内では既存のQApplicationを利用します。テストは一時フォルダーに画像・出力を作成し、終了時に削除します。
