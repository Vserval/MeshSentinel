# 平面投影後の「非多様体UV」対策 — 1.7.1-rc1

## 更新と使い方

1. 作業シーンを別名保存し、Mayaを終了します。
2. 旧版のMeshSentinelフォルダーをバックアップし、このZIPのMeshSentinelフォルダーに置き換えます。旧版と新版のファイルを混在させないでください。
3. Mayaを起動し、MeshSentinel内のinstall.pyをビューポートへドラッグ＆ドロップします。
4. 形状修復やマージを終え、UVを平面投影します。
5. 対象メッシュを選択し「UV展開エラーを修復（三角化なし）…」を開きます。
6. 「修復後にUnfold3Dで展開する」をオンにして「三角化せずに修復」を押します。

修復のみを実行し、続けてMayaの「展開」を使うこともできます。修復後に再度平面投影した場合は修復からやり直してください。

## Pythonから直接実行

インストール後、対象メッシュを選択して、MayaスクリプトエディタのPythonタブで実行します。

```python
from mesh_sentinel.uv_repair import repair_and_unfold_selected
repair_and_unfold_selected()
```

形状の頂点接続を変えたくない場合:

```python
repair_and_unfold_selected(split_geometry=False)
```

この場合、形状側に非多様体があれば変更せず停止します。コンポーネント選択でも、そのコンポーネントが属するメッシュ全体が対象です。複数選択時は全対象を一つのUndo単位で処理します。

## 変更されるもの・されないもの

- 三角化・面削除・頂点移動は行いません。四角形/N-gonの面構成と面コーナー位置を検証します。
- UVの異常な接続をカット／再割り当てします。UV数・シーム・シェル構成が変わる場合があります。
- 形状自体の非多様体分離を許可した場合は頂点数が増え、接続部に切れ目ができます。
- 修復段階は既存UV座標を検証します。Unfoldを選ぶと現在UVセットの座標は変わります。パッキングは行いません。
- 診断が特定できない問題や検証エラーは成功扱いにせず停止し、処理中のエラーでは同一Undo単位の取り消しを要求します。
- Undoを有効にしてください。制作リグや履歴が複雑なメッシュには必ずコピーで試してください。

## 検証と限界

Maya非依存テスト66件が成功しました。追加25件のうち10件はコマンドをモック化したテストです。Maya実機、UI表示、Unfold3Dでの動作、ユーザーの問題メッシュでの解消は未確認です。

Mayaでの追加テストはtests/integration_uv.pyに同梱しています。既存テスト同様、専用のmayapyプロセスで実行してください。テストはシーンを初期化するため、制作中のMayaで実行しないでください。

```text
mayapy tests/integration_uv.py <テスト出力フォルダー>
```

UVの座標が重なっているだけのケースは異常なID共有と区別します。穴付きポリゴンを含むメッシュは追加の独立診断の対象外です。ゼロ面積・自己交差・破損履歴など、すべてのUnfold失敗を直す機能ではありません。エラーが残る場合は、Mayaのバージョン、今回の修復結果、問題部分だけのメッシュを確認する必要があります。
